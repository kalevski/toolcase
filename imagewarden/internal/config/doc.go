// Package config loads and validates the imagewarden configuration: defaults,
// overlaid by the YAML file, overlaid by IMAGEWARDEN_* environment variables
// (see env.go for the variable table).
package config
