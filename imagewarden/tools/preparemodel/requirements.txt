# Dev-machine / CI-model-job only — NEVER installed in the Docker build or at
# runtime (spec §3.2, §9.1; see README.md). Pinned so the ONNX graph this tool
# emits is guaranteed loadable by the container's ONNX Runtime (spec §3.4):
# onnxruntime here must stay on the same 1.20.x line as the runtime .so.
tf2onnx==1.16.1
onnxruntime==1.20.1
tensorflow==2.16.1
PyYAML==6.0.2
