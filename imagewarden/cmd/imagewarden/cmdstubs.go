package main

import "fmt"

// Temporary stub for the classify subcommand implemented by task 026
// (cmdClassify). cmdRun moved to run.go (task 024); cmdValidate moved to
// validate.go (task 025); cmdSchema moved to schema.go (task 027);
// cmdHealthcheck moved to healthcheck.go (task 028). Each sibling task
// replaces its stub with a real implementation in its own file; remove the
// corresponding case here once that happens.

func cmdClassify(args []string) int {
	fmt.Println("imagewarden classify: not implemented")
	return 0
}
