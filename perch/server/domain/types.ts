// Pure shared domain types — safe to import from client AND server, so this file
// deliberately has NO `import 'server-only'` and performs no I/O. No types yet.
// See notes/static-hosting-app-design.md §6, §12.

export {}
