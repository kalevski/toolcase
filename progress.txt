# Ralph progress log

Append-only learnings from each iteration. Future iterations read this to
avoid repeating mistakes and to inherit conventions discovered along the way.

## Iteration 1 — opt-examples-public-base
- Added Packing section (Packer, 5 algorithms, helpers) — namespace was fully exported from base/src/main.ts but absent from SKILL.md.
- Pattern: cross-check `src/main.ts` exports against SKILL.md TOC. Re-exported namespaces (HTTP, Packing) often hide coverage gaps. Public types are re-exported with `Packing*` prefix — list them when adding a section.

## Iteration 2 — opt-examples-public-game-components
- Frontmatter "139 components" + body "all 134" → corrected to 137 (count of `customElements.define` calls in `register.ts`; ResourceBarBase + SettingRowBase are abstract, not registered).
- Fixed peer dep `@toolcase/base 2.x` → `3.x.x` (per package.json).
- Anchor table claimed slot-based positioning ("via slots/CSS") — actually uses `position` + `inset` attrs, single `<slot>`. Rewrote the layout example to use one `<gc-anchor position="…">` per corner.
- `gc-card` doesn't exist — replaced with `gc-panel` in grid example.
- InventoryGrid example used `rows="6"` + `item-hover` + `item-click` (none real). Real API: attrs `columns`/`slot-size`/`selected-id`, prop `items`, single `select` event `{ item, index }`.
- Minimap example used `zoom` attr + `mini.center =` (don't exist) + slotted markers (Minimap ignores children). Real API: `world-x/y/width/height` + `markers` prop (data-driven).
- Pattern: for component-heavy packages, spot-check `observedAttributes` and event detail shapes — class-name + attribute-list mismatches are the most common form of staleness, more than missing exports.

## Iteration 3 — opt-examples-public-logging
- Dropped duplicate "Independent factory" LoggerFactory example (verbatim repeat of an earlier audit-factory block).
- Removed redundant "Custom reporter" subsection inside the LogReporter API section — the four worked custom-reporter examples below already cover it; collapsed to a one-line sync/throws-bubble note.
- Replaced hand-rolled `RemoteReporter` (manual queue + setTimeout) with a `BufferedReporter({ onFlush })` example since the built-in already implements batching/debounce.
- Dropped the standalone "File reporter (Node)" custom-LogReporter example — `FileLogReporter` + the JSONLineReporter-over-createWriteStream combo earlier in the doc already cover Node file output.
- Pattern: when the doc has both an API section AND a worked-examples section, the API-section "custom X" subblock almost always duplicates one of the examples — drop the API-side copy, keep the worked examples.
- All exports cross-checked: `main.ts` exports `{ logging, Logger, Level, LoggerFactory, LogReporter, ConsoleLogReporter, JSONLineReporter, BufferedReporter }`; `node.ts` exports `{ FileLogReporter }`. All covered in doc.

## Iteration 4 — opt-examples-public-next-static-app
- Doc already factually accurate: react-components primitives (Hero/CoolNav/CoolButton/Brand/PageFooter/PricingCard/FeatureCard/PinnedFeatureShowcase/EarlySignupForm/Heading/Text/SectionCard) all exist in react-components/src/index.ts; HeroProps + CoolNavProps shapes match the examples; cross-skill `react-spa-app` reference resolves to examples/public/react-spa-app/.
- Surgical hedge-stripping only — dropped "generally", "actually", "just", "usually" / "definitely" from 5 spots without touching technical substance.
- Pattern: architecture-blueprint SKILL.md files (under examples/public/) are not tied to a single package, so the "every export reachable" coverage check doesn't apply — instead verify every named library symbol/prop in the doc against the dependency's `src/index.ts` + interface file. Hedges are the main lever for tightening these.

## Iteration 5 — opt-examples-public-node-service
- Fixed @toolcase/serializer wire/ example: doc used non-existent `new Schema('X', { field: { id, type: Type.STRING } })` API. Real API is `new Serializer(id?)` + `wire.define(key, [{ key, type: Serializer.FieldType.STRING, rule }])`. Tags are positional (insertion order), not id-on-field. Also added pointers to versioning (`encodeVersioned`/`migrate`) and fragment/reassemble.
- Fixed Transports.WSTransport constructor: doc merged ticketSource into ws.ServerOptions object. Real signature is positional: `new Transports.WSTransport(serverOptions, queryTicketParam, transportOptions)`. ticketSource lives in third arg.
- Dropped invalid `env<T>(name, default, type)` generic — `env` is overloaded, not generic. Cleaned env.ts example accordingly and added a one-line signature note.
- Restructured "@toolcase/base" bullet that confusingly attributed `env` to base. Split into `@toolcase/base` (zero-dep + HTTP namespace) and `@toolcase/node` (env + Fastify/Kysely/Redis/sharp helpers). Listed real exports: HttpServer, BaseRepository, SoftDeleteRepository, EntityService, KVService, ImageProcessor, AtlasBuilder, OAuth2, RESTRouteHandler.
- Added `@toolcase/logging/node` `FileLogReporter` mention — subpath export verified in logging/package.json.
- Dropped duplicate "Migrations (out-of-tree)" subsection (4 bullets that all restated points already made in stack baseline / db/ folder header / boot rules / anti-patterns).
- Tightened: dropped "Every new project... Deviation = bug." restatement; "explicit is better"; "almost never what you want"; "the single rule that catches most violations:".
- Pattern: serializer is the canonical "API in the docs is *not* the API in the code" trap — when you see schema-builder DSL examples in a SKILL.md, grep the package's main.ts class for the actual surface before trusting the example.

## Iteration 6 — opt-examples-public-node
- @toolcase/node SKILL.md had a wall of API drift: `createAPISanitizer`/`APISanitizers`/`APISanitizerOptions`/`APISanitizer` → real names are `createSanitizer`/`SanitizerVisitors`/`SanitizerOptions`/`Sanitizer` (with separate `InputCallOptions`/`OutputCallOptions`/`QueryCallOptions`).
- `statusCodeFromError` + `codeFromError` don't exist — replaced with `errorMeta(e): { code, status } | null` (single function carrying both).
- Repo + service rename pass: BaseRepository's `list/findWithCount/findPage/findCursorPage/findFirst` → `findMany/findManyWithCount/paginate/paginateCursor` (no `findFirst` at all). Added `findByIdMany`. `findManyWithCount` returns `{ results, total }`, not `[Selectable[], total]`. SoftDeleteRepository: `listActive`/`findActivePage` → `findManyActive`/`paginateActive`.
- EntityService: added `upsert`, removed `findFirst`, fixed list/page method names. Noted `beforeInsert`/`beforeUpdate` MUST return values; `afterInsert`/`afterUpdate` MUST return rows.
- RESTRouteHandler: `rejectEmptyPatch` (default true) was actually `allowEmptyPatch` (default false) — opposite name, same end behavior; opt-IN to allow rather than opt-OUT to reject. RESTRouteHandler list endpoint calls `service.paginate`, not `service.findPage`.
- RouteHandler.mapError is public, not protected (the public method is the wrap-target for OTel/Sentry composers).
- `chain(...endpoints)` doesn't exist. Only `Router` is exported. Dropped from imports + section.
- `parseFilters`/`parseSort` are internal-only (utils/ but not re-exported from main.ts). Repurposed their sub-section as RESTRouteHandler URL-syntax docs, with explicit "not re-exported" note.
- KVService field rename: `readonly values: ValueStore` → `readonly objects: ValueStore`.
- SubscriberPool's public method is `subscribeRaw(channel, handler: (Buffer, Buffer) => void)`, not `subscribe(channel, handler)`.
- OAuth2: `oauth2Provider` doesn't exist — real export is `defineOAuth2Provider`. Replaced all 3 call sites (intro, GitHub snippet, Discord snippet).
- `EventEmitter` is from `@toolcase/base`, not `@toolcase/node` — fixed the device-flow import block.
- `kv.set` cookbook example used positional `'EX', STATE_TTL` — real signature is `set(key, value, options?: SetOptions)`, so options must be `{ EX: STATE_TTL }`.
- env signature: was a fake generic `env<T>(key, default, type)`. Real is 6-overload function (string|number|boolean × default-typed/null). Replaced doc signature with the overload list.
- Added `QueryEvent` + `QueryHook` + `onQuery` to BaseRepositoryOptions (tracing hook was missing).
- Pattern: this SKILL.md had drifted across nearly every section because the underlying package renamed/refactored repo-method names AND sanitize types in lockstep. When you see one rename (e.g. `findPage`→`paginate`), grep the whole doc for the old name — they usually appear in BaseRepository, EntityService, AND the RESTRouteHandler routing table.

## Iteration 8 — opt-examples-public-phaser-plus
- Bumped peer ranges: @toolcase/base ^2.x → ^3.x, @toolcase/logging ^2.x → ^3.x. Added optional react/react-dom peers (per package.json).
- Engine: "Per-Scene singleton" → "Per-Game singleton attached to game.engine" (initializeEngine() reuses one Engine across scenes).
- "84 effects" → "73 effects" (counted EFFECT_REGISTRY: 16 color + 13 procedural + 13 distortion + 7 dissolve + 8 mask + 16 lighting).
- LetterboxFeature API: doc had `show(size, sec)` / `hide(sec)` — neither exists. Real surface: setAspect(ratio) / setBarColor / setBarDepth; bars draw automatically when viewport ≠ targetAspect. Fixed section + cheat sheet row.
- GamepadFeature: `setDeadzone(0.15)` → `setDefaultDeadZone(0.15)` (also has setDeadZone(padIndex, value)); `vibrate(...)` → `rumble(padIndex, strongMag, weakMag, durationMs)`. Added setDefaultMapping/setMapping/axes coverage + GAMEPAD_* + MAPPING_* events/presets.
- VirtualJoystick: `setPosition`/`setRadius`/`stick.x`/`stick.y` don't exist. Real: `setJoystickConfig({ size, deadZone, placement, bottomOffset, sideOffset })`, `setInput(InputFeature | null)`, `addButton(config)`, `stick.vector.x`/`.y`. Fixed section + cheat sheet row.
- GestureRecognizer: doc used raw strings `'swipe'`/`'double-tap'`/`'pinch'`. Real events are constants `GESTURE_SWIPE`/`GESTURE_DOUBLE_TAP`/`GESTURE_PINCH`/`GESTURE_TAP`/`GESTURE_LONG_PRESS`. Switched example to the constants.
- StateMachine constants deep-import `'@toolcase/phaser-plus/lib/flow/StateMachine'` → just `'@toolcase/phaser-plus'` (package.json `exports` only allows `.`; deep imports break consumers and STATE_* are already re-exported via flow/index.ts → top-level barrel).
- PATH_FAILED reason: was `'blocked' | 'exhausted'` in top block — fixed to `'end_blocked' | 'exhausted' | 'max_iterations'` matching PathFinder source.
- Theming: dropped `ReactFeature` row — features/index.ts exports it but the top-level src/index.ts deliberately does NOT re-export from features/index.ts (it imports each feature individually, sans ReactFeature), so `import { ReactFeature } from '@toolcase/phaser-plus'` would fail. Also dropped the made-up `--tc-dbg-*` Debugger CSS vars (Debugger is Tweakpane, not a custom CSS layer).
- Custom-effect example: dropped manual `ensureEffectRegistered(game, MyEffect)` — function is internal-only (not in any package barrel). EffectManager.add() already calls it automatically on first add.
- Expanded top-level import block to enumerate input feature surface (InputFeature/InputBuffer/GamepadFeature/VirtualJoystick + their constants), cinema easing + SHOT_DONE, and Flow extras (STATE_*, SUCCESS/FAILURE/RUNNING, REPLAY_*) — they were exported from `flow/index.ts` + barrels but invisible in the doc's overview.
- Pattern: when the top of a SKILL.md has a "single import surface" block, treat it as a coverage spec — diff it against `src/index.ts` re-exports and every barrel re-exported from it. Constants (GESTURE_*, MAPPING_*, EASE_*) are the easiest things to lose; they leak only via barrels.

## Iteration 7 — opt-examples-public-phaser-game-dev
- Stack baseline: bumped peer ranges to actual (phaser-plus ^1.x → ^3.x; @toolcase/base ^2.x → ^3.x; @toolcase/logging ^2.x → ^3.x). Reworded exports list to match real top-level + re-export shape (Engine/Scene/.../SplitScreen, plus Scene2D/World/GameObject2D from perspective2d, installEffects from effects). Node 20+ → Node 18+ per engines.
- Component count: every "134" → "137" (count of customElements.define calls in register.ts). Frontmatter description also updated.
- Anchor API: doc treated gc-anchor as a multi-slot container with `slot="top-left"` kids. Real Anchor has single `<slot>` and a `position` attr — one anchor per corner. Rewrote HUDFeature, HotbarFeature, MinimapFeature, DialogueFeature to use one `<gc-anchor position="...">` per corner.
- gc-panel doesn't take a `header` attr (real attrs: bordered/corners/parchment). Replaced `<gc-panel header="X">` with `<gc-panel bordered><gc-panel-header>X</gc-panel-header>` in inventory + settings.
- DialogueBox attrs `speaker`/`text`/`typing-speed` (NOT `typewriter`). Box also exposes `.text` JS property; setting `.textContent` skips the typing pipeline. Fixed DialogueFeature.
- InventoryGrid: no `rows` attr; only `select` event with `{ item, index }`. Removed `rows="6"` and merged `item-hover`/`item-click` handling into single `select` listener.
- Minimap: world via `world-x/y/world-width/world-height` attrs, markers via `.markers` array prop (NOT slotted gc-objective-marker children, NOT a `zoom` attr, NOT `.center =`). Rewrote MinimapFeature to slide the world window and accumulate markers.
- GameOverScreen extends ResultScreen; emits `action` event with `{ id }`, not `continue`. Fixed.
- BlurOverlay: no `open` attr (toggling visibility is the host's job — added explicit `style.display` toggle). Theming example: real CSS var is `--gc-blur-amount`, not `--gc-blur-overlay-amount`.
- Settings rows extend SettingRowBase with attr `row-label`, not `label`. GraphicsPresetPicker uses `value` not `preset`. Fixed.
- Pool API: GameObjectPool exposes `register(key, class, instanceFn?, resetFn?)` + `obtain<T>(key)` + `release(obj)`. No `.add()`, no `.spawn(key, x, y)`, no `{ initial, max }` opts. ObjectLayer.add(key, x, y) is the layer-bound positioned-spawn API (internally pool.obtain + setPosition). Fixed pool section + Recipes + Anti-patterns + MainScene example.
- ServiceRegistry method is `resolve(class)`, not `.get(class)`. Also has `bind`/`provide`/`has`/`dispose`. Fixed services section + recipe.
- InputFeature: query method is `isPressed(action)`, not `isDown(action)`. Binding is `bind(action, [Binding[]])`. Fixed.
- Pattern: architecture-blueprint SKILL.md files compound staleness — when phaser-plus + game-components both moved to 3.x peer ranges and several gc-* components renamed attrs in lockstep, the doc has drift in every example. Worth grepping the doc for *all* old attribute names from one component when fixing it (e.g. once you see `rows=`, also check `header=`, `preset=`, `label=`, `slot="…"`).

## Iteration 9 — opt-examples-public-react-components
- Frontmatter description: dropped fragile "Covers 179 components across…" count, replaced with concrete trigger surface (React 18+/Bootstrap 5, named-component triggers, theme--neon).
- Added missing `### Card` body section between Breadcrumb and CommandPalette. TOC had `[Card](#card)` link but no anchor existed — broken anchor for a real top-level export (`src/Card.tsx`: header/variant/loading/className).
- Hero example drift: `primaryAction={{ href: '/docs', ... }}` + `external: true` — HeroAction extends `ButtonHTMLAttributes<HTMLButtonElement>` (renders as `<Button>`, no href/link). Switched to `onClick` + window.open pattern and added a clarifying note on HeroAction shape.
- Hero `bgIcons` was documented as "Bootstrap Icon names" but the source maps each entry to `<img src={src}>` (literal URLs). Fixed description + example.
- Pattern: for components that look "link-like" (Hero/CTA/CoolButton), grep the source for `<a` or `href` before trusting any example that uses `href` — many of these are pure `<button>` underneath and the doc gets it wrong.

## Iteration 10 — opt-examples-public-react-spa-app
- Architecture-blueprint doc; trimmed @toolcase library bullets to factual surface.
- Fixed @toolcase/base attribution: removed bogus "Node-only env() loader" (env lives in @toolcase/node, not base). Added missing real exports: LSystem, WeightedRandom, Dijkstra/AStar, AdjacencyMatrix, State, Packing namespace, HTTP sub-symbols (Status/RESTError/RESTResponse).
- @toolcase/logging: dropped "Tiny" marketing adjective; named the concrete reporters (ConsoleLogReporter default, JSONLineReporter, BufferedReporter) instead of generic "console default; swap for remote, file, or custom".
- @toolcase/serializer: replaced generic prose with the actual call shape (`new Serializer()` + `wire.define(key, [{ key, type, rule }])`) since iter 5 already flagged the schema-builder DSL pitfall.
- @toolcase/react-components: "160+" → "180+" (counted 183 `export * from './X'` lines in src/index.ts).
- Pattern: when a blueprint SKILL.md surfaces *neighbour packages*, the bullet for each package is the first place to verify against that package's main.ts — they age faster than the architecture rules themselves because they get rebranded ("primary choice for…") and accumulate stale primitive names. Treat each one-line library description as a mini-coverage check.

## Iteration 11 — opt-examples-public-serializer
- Streaming example used undefined `peekTotal(chunk)` helper — inlined it as `(chunk[6] << 8) | chunk[7]` (the chunk-header total bytes per the fragment() format spec already in the doc).
- Defensive decode section claimed `decode() throws 'decode error: ...'` — wrong template; real format is `Serializer.decode[<key>] failed: <reason> (bytes=<size>, offset=<pos>)` (already correctly documented in the decode API block above). Also dropped the bespoke `safeDecode<T>(key, buf)` try/catch wrapper since the class already exposes `safeDecode(key, buffer): SafeResult<…>` — having both is confusing duplication and the example shadowed the real method name.
- All exports cross-checked against `serializer/src/main.ts`: default + named Serializer, plus types FieldType/SafeResult/EnumMarker/MapMarker/PackedMarker/FieldTypeRef/Version/VersionedFrame/MigrationFn. All public methods (define, enum, validate, encode, decode, safeEncode, safeDecode, version, getVersion, migrate, encodeVersioned, decodeVersioned, types, fields, fragment, reassemble) covered.
- Pattern: when the doc has a defensive/error-handling section AND the package ships a built-in `safe*` envelope variant, prefer the built-in over hand-rolled try/catch — the latter usually introduces a duplicate-name shadow (here `safeDecode` collided with `Serializer.safeDecode`) and goes stale on error-template changes.
