// lucide-static publishes `typings: dist/lucide-static.d.ts` but the file is
// missing from the tarball. Shorthand ambient declaration: every named export
// (a raw SVG string) types as `any`.
declare module 'lucide-static'
