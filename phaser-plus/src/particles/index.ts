import ParticleFeature from './ParticleFeature'
import type { ParticlePreset, StreamHandle } from './ParticleFeature'
import {
    PARTICLE_BURST,
    PARTICLE_STREAM_START,
    PARTICLE_STREAM_STOP
} from './ParticleFeature'

export {
    ParticleFeature,
    PARTICLE_BURST,
    PARTICLE_STREAM_START,
    PARTICLE_STREAM_STOP
}

export type {
    ParticlePreset,
    StreamHandle
}
