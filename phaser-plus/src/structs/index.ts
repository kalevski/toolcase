import { Spatial } from '@toolcase/base'

const SpatialHash = Spatial.SpatialHash
const Quadtree = Spatial.Quadtree

export { SpatialHash, Quadtree }
export type { SpatialPoint, SpatialRect } from '@toolcase/base'
